## For windows
Accept unsigned scripts ``Set-ExecutionPolicy Bypass -Scope Process``
Launch the ``tools/dev-networks.ps1`` script inside an administrator PWS shell

## Deployment
RPI must have docker installed, you can install it via `curl -fsSL https://get.docker.com | sh`

## To Do
### Provisionning a new rpi
- add ssh key
- install vpn
- install docker and make it start automatic
- add default user to docker group
